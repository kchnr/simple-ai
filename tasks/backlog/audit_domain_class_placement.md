USER STORY: Audit Core Domain Class Placement
As a developer, I want to audit core domain class placement, so that domain logic is correctly encapsulated and no relocation is needed.

Raw Notes:
- Audit core domain class placement: confirm `gen.ai.Genome`, `gen.ai.evolution`, `gen.ai.network`, and `gen.ai.tasks` packages correctly encapsulate domain logic and require no relocation. 