USER STORY: Refine Neuron Data Class
As a developer, I want to review and refine the `Neuron` data class in `Genome.kt`, so that it appropriately supports MVP requirements and future evolution.

Raw Notes:
- Review and refine `Neuron` data class in `Genome.kt` (ID, type, activation function, initial state, plasticity flags, learning rate, threshold, bias, time constant, neuromodulator sensitivity, innovation ID).
- Ensure all fields are justified for MVP or clearly marked for future phases.
- Consider if `initial state` or `time constant` can be leveraged by evolution to approximate aspects of neuronal state dynamics (e.g., simple forms of membrane potential decay or refractoriness). 